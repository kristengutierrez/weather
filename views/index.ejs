<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Test</title>
    <!-- <link rel="stylesheet" type="text/css" href="/assets/styles/salesforce-lightning-design-system.css" /> -->
    <link rel="stylesheet" type="text/css" href="/style.css">
  </head>
  <body background="">
    <div class="container">
      <img src="/images/penweather.jpg" alt="Penrod Weather" class="center">
      <fieldset>
        <form action="/" method="post">
        </form>
        <% if(locals.weather !== null){ %>
            <p><%= locals.weather %></p>
          <% } %>
          <% if(locals.error !== null){ %>
            <p><%= locals.error %></p>
          <% } %>
      </fieldset>
    </div>
  </body>
</html>